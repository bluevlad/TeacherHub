# TeacherHub Local Development Environment
# Copy to .env.local and adjust as needed
# Usage: cp .env.local.example .env.local

# Database (PostgreSQL - docker-compose.local.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=teacherhub

# Application
APP_MODE=development
