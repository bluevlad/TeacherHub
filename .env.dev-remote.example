# ==============================================
# 개발서버 → 운영 DB 원격 접속 환경변수
# ==============================================
#
# 사용법:
#   cp .env.dev-remote.example .env.dev-remote
#   .env.dev-remote 파일에서 <운영DB비밀번호>를 실제 값으로 변경
#
# 사전 준비:
#   SSH 터널: ssh -L 15432:localhost:5432 <user>@study.unmong.com -N -f

# DB 접속 (SSH 터널 경유)
DB_HOST=host.docker.internal
DB_PORT=15432
DB_USER=teacherhub
DB_PASSWORD=<운영DB비밀번호>
DB_NAME=teacherhub

# 실행 모드: ai-only (스케줄러/크롤링 비활성화, DB 접속만 유지)
APP_MODE=ai-only
